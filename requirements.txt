# AUIS Academic Catalog RAG Application - Requirements
# Install with: python -m pip install --user -r requirements.txt
# 
# Note: Install PyTorch with CUDA separately first:
# python -m pip install --user torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

transformers>=4.40.0
accelerate>=0.28.0
chromadb>=0.4.24
sentence-transformers>=2.6.0
pdfplumber>=0.11.0
flask>=3.0.0
flask-cors>=6.0.0

# Optional (not currently used):
# bitsandbytes>=0.43.0
# gradio>=4.26.0
